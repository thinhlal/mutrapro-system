apiVersion: v1
kind: Service
metadata:
  name: studio-service
  namespace: mutrapro
  labels:
    app: studio-service
spec:
  selector:
    app: studio-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
