apiVersion: v1
kind: Service
metadata:
  name: studio-service
  namespace: mutrapro
  labels:
    app: studio-service
spec:
  selector:
    app: studio-service
  ports:
  - name: http
    port: 8087
    targetPort: 8087
    protocol: TCP
  type: ClusterIP
