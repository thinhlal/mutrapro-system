apiVersion: v1
kind: Service
metadata:
  name: request-service
  namespace: mutrapro
  labels:
    app: request-service
spec:
  selector:
    app: request-service
  ports:
  - name: http
    port: 8084
    targetPort: 8084
    protocol: TCP
  type: ClusterIP

