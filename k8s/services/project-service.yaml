apiVersion: v1
kind: Service
metadata:
  name: project-service
  namespace: mutrapro
  labels:
    app: project-service
spec:
  selector:
    app: project-service
  ports:
  - name: http
    port: 8082
    targetPort: 8082
    protocol: TCP
  type: ClusterIP
