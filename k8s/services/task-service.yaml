apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: mutrapro
  labels:
    app: task-service
spec:
  selector:
    app: task-service
  ports:
  - name: http
    port: 8092
    targetPort: 8092
    protocol: TCP
  type: ClusterIP
