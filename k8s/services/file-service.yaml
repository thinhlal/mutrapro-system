apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: mutrapro
  labels:
    app: file-service
spec:
  selector:
    app: file-service
  ports:
  - name: http
    port: 8086
    targetPort: 8086
    protocol: TCP
  type: ClusterIP
