package com.mutrapro.project_service.enums;

public enum ContractType {
    transcription,
    arrangement,
    arrangement_with_recording,
    recording,
    bundle  // Transcription + Arrangement + Recording (full package)
}

