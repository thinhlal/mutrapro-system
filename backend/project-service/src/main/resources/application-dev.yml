server:
  port: 8082
  servlet:
    context-path: 

spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  datasource:
    url: jdbc:postgresql://shinkansen.proxy.rlwy.net:43102/railway
    username: postgres
    password: WEMkCGvTPJngCDSicuOItQDUsKCQRUDA
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  sql:
    init:
      mode: never
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: project-service-file-consumer
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      enable-auto-commit: false
      properties:
        spring.json.trusted.packages: "*"

logging:
  level:
    root: INFO
    com.mutrapro.project_service: DEBUG

# JWT Configuration - Must match auth-service signerKey
jwt:
  signerKey: QVHfEyXEd7KG4eUfYAWOUvuPjlufU3vImJ0MEialEhHoQPjB6wZTL6Ma9XLnKaYn

# Event Topics Configuration
app:
  event-topics:
    mappings:
      file.uploaded: file-uploaded
      contract.otp.email: contract-otp-email
      contract.signed: contract-events
      contract.signed.email: contract-signed-email
      billing.deposit.paid: billing-deposit-paid
      billing.milestone.paid: billing-milestone-paid
      billing.revision.fee.paid: billing-revision-fee-paid
      task.assignment.assigned: task-assignment-assigned
      task.assignment.ready.to.start: task-assignment-ready-to-start
      task.assignment.canceled: task-assignment-canceled
      task.issue.reported: task-issue-reported
      contract.notification: contract-notification
      submission.delivered: submission-delivered
      revision.requested: revision-requested
      revision.delivered: revision-delivered
      revision.submitted: revision-submitted
      revision.approved: revision-approved
      revision.rejected: revision-rejected
      milestone.paid.notification: milestone-paid-notification
      milestone.ready.for.payment.notification: milestone-ready-for-payment-notification
      billing.revision.fee.refunded: billing-revision-fee-refunded # New topic for refunded revision fees
      chat.system.message: chat-system-message-events

# Request Service Configuration
request:
  service:
    base-url: http://localhost:8084

# Chat Service Configuration
chat:
  service:
    base-url: http://localhost:8088

# Notification Service Configuration
notification:
  service:
    base-url: http://localhost:8085

# Billing Service Configuration
billing:
  service:
    base-url: http://localhost:8083

# Specialist Service Configuration
specialist:
  service:
    base-url: http://localhost:8086

# AWS S3 Configuration
aws:
  s3:
    enabled: true
    bucket-name: mutrapro-dev-files
    region: ap-southeast-1
    access-key: AKIAZ24ITCMNFIY2Z3VX
    secret-key: 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

# E-Signature Configuration
esign:
  otp:
    expiration-minutes: 5
    max-attempts: 3
    length: 6

# Management & Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true