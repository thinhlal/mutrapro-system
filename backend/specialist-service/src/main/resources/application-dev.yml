server:
  port: 8086
  servlet:
    context-path: 

spring:
  datasource:
    url: jdbc:postgresql://switchyard.proxy.rlwy.net:23349/railway
    username: postgres
    password: KsAAqFfYPNVMTLuxMobTSYtNMiWHfeTD
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
  sql:
    init:
      mode: never
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: specialist-service
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      enable-auto-commit: false
      properties:
        spring.json.trusted.packages: "*"
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB  # Maximum size per file (50MB) - cho demo audio files
      max-request-size: 50MB  # Maximum size per request (50MB)
      file-size-threshold: 2KB  # Size threshold after which files will be written to disk

logging:
  level:
    root: INFO

# JWT Configuration - Must match auth-service signerKey
jwt:
  signerKey: QVHfEyXEd7KG4eUfYAWOUvuPjlufU3vImJ0MEialEhHoQPjB6wZTL6Ma9XLnKaYn

# AWS S3 Configuration
aws:
  s3:
    enabled: true
    region: ap-southeast-1
    access-key: AKIAZ24ITCMNFIY2Z3VX
    secret-key: 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
    bucket-name: mutrapro-dev-files

# Project Service Configuration
project:
  service:
    base-url: ${PROJECT_SERVICE_BASE_URL:http://localhost:8082}

# Identity Service Configuration
identity:
  service:
    base-url: ${IDENTITY_SERVICE_BASE_URL:http://localhost:8081}

# Event Topics Configuration
app:
  event-topics:
    mappings:
      slot.booked: slot-booked-events
      slot.released: slot-released-events
      review.created: review-created-events

# Management & Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true

