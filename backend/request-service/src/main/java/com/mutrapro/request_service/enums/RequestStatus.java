package com.mutrapro.request_service.enums;

public enum RequestStatus {
    pending,
    contract_sent,
    contract_signed,
    approved,
    in_progress,
    completed,
    cancelled,
    rejected
}

