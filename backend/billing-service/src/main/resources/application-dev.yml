server:
  port: 8083
  servlet:
    context-path: 

spring:
  datasource:
    url: jdbc:postgresql://tramway.proxy.rlwy.net:31325/railway
    username: postgres
    password: IcnZyTTtdUyHtFYAkZFyRUCYDeLfdZQM
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
  sql:
    init:
      mode: never
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: billing-service
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      enable-auto-commit: false
      properties:
        spring.json.trusted.packages: "*"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
      properties:
        enable.idempotence: true

logging:
  level:
    root: INFO

# JWT Configuration - Must match auth-service signerKey
jwt:
  signerKey: QVHfEyXEd7KG4eUfYAWOUvuPjlufU3vImJ0MEialEhHoQPjB6wZTL6Ma9XLnKaYn

# Event Topics Configuration
app:
  event-topics:
    mappings:
      billing.deposit.paid: billing-deposit-paid
      billing.milestone.paid: billing-milestone-paid
      billing.revision.fee.paid: billing-revision-fee-paid
      billing.revision.fee.refunded: billing-revision-fee-refunded # New topic for refunded revision fees
      payment.order.completed.notification: payment-order-completed-notification # Notification khi payment order completed

# Management & Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true

# SePay Configuration
sepay:
  api:
    key: 7CJ7XQ0EVOJUFVLZ4ROGJZ6DBEVVFHFY2NWYKBMR894XPBF11UOELCGM5XIYACKR  # API Key để xác thực webhook
  account:
    number: VQRQACQGL7610  # Virtual Account (VA) nhận tiền từ SePay
    bank: MBBank  # Mã ngân hàng
  order:
    expiry:
      minutes: 30
    prefix: MTP-TOPUP  # Prefix cho mã đơn hàng trong nội dung chuyển khoản